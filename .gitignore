# Local .terraform directories
**/.terraform/*

# terraform.tfvars
*.tfvars

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.log.*

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data based configuration files and should be treated like
# secrets.
#
# If you intentionally want to track certain .tfvars files (e.g. shared defaults),
# you can use "!example.tfvars" to exempt them.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include plugin cache directory
.terraform/plugin-cache/*

# Ignore CLI configuration files
.terraformrc
terraform.rc

