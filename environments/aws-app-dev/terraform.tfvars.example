# Copy to terraform.tfvars and fill in.
# Or generate from your default SSH key:
#   cp terraform.tfvars.example terraform.tfvars
#   # Replace the ssh_public_key value with output of: cat ~/.ssh/id_rsa.pub

aws_region = "ap-northeast-2"
project_name = "mek-lab"
environment  = "app-dev"
vpc_cidr   = "10.0.0.0/16"
public_subnet_cidr = "10.0.1.0/24"
key_name = "mek-lab-app-dev-key"

# Paste your SSH public key (e.g. contents of ~/.ssh/id_rsa.pub)
ssh_public_key = "ssh-rsa AAAA... your-key"

# Optional: restrict access to your IP.
# allowed_ssh_cidrs  = ["1.2.3.4/32"]
# allowed_http_cidrs = ["1.2.3.4/32"]
# allowed_app_cidrs  = ["1.2.3.4/32"]

# Sizing for requested target (2 vCPU, 8 GB RAM, gp3 disk 100-200 GB)
instance_type       = "t3.large"
root_volume_size_gb = 150
root_volume_type    = "gp3"

tags = {
  Owner = "platform-team"
}
